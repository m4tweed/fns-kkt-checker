# 🚀 Деплой на Vercel

Это руководство поможет развернуть проект на Vercel.

## Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com) (бесплатный)
2. Аккаунт на [GitHub](https://github.com)
3. Git установлен локально

## Шаг 1: Подготовка репозитория

### 1.1 Инициализация Git (если еще не сделано)

```bash
cd fns_kkt_checker
git init
git add .
git commit -m "Initial commit"
```

### 1.2 Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий (например, `fns-kkt-checker`)
4. **НЕ** инициализируйте с README (у нас уже есть файлы)
5. Нажмите "Create repository"

### 1.3 Связывание с GitHub

```bash
git remote add origin https://github.com/ваш-username/fns-kkt-checker.git
git branch -M main
git push -u origin main
```

## Шаг 2: Деплой на Vercel

### Вариант A: Через Web интерфейс (рекомендуется)

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New Project"**
3. Выберите **"Import Git Repository"**
4. Найдите и выберите ваш репозиторий `fns-kkt-checker`
5. Vercel автоматически определит настройки:
   - **Framework Preset**: Other
   - **Root Directory**: ./
   - **Build Command**: (оставьте пустым)
   - **Output Directory**: ./
6. Нажмите **"Deploy"**
7. Подождите 1-2 минуты ⏳
8. Готово! 🎉

### Вариант B: Через Vercel CLI

```bash
# Установка Vercel CLI
npm i -g vercel

# Вход в аккаунт
vercel login

# Деплой
vercel

# Следуйте инструкциям:
# - Set up and deploy? Yes
# - Which scope? (выберите ваш аккаунт)
# - Link to existing project? No
# - What's your project's name? fns-kkt-checker
# - In which directory? ./
# - Want to override settings? No

# Деплой в продакшн
vercel --prod
```

## Шаг 3: Проверка работы

После деплоя Vercel предоставит URL вида:
```
https://fns-kkt-checker.vercel.app
```

Откройте этот URL и проверьте:
- ✅ Страница загружается
- ✅ Можно выбрать модель ККТ
- ✅ Можно ввести номера касс
- ✅ Проверка работает
- ✅ Экспорт в CSV работает

## Автоматический деплой

После настройки каждый `git push` в ветку `main` будет автоматически деплоить новую версию! 🚀

```bash
# Внесли изменения
git add .
git commit -m "Описание изменений"
git push

# Vercel автоматически задеплоит через 1-2 минуты
```

## Структура проекта для Vercel

```
fns_kkt_checker/
├── api/                    # Serverless функции
│   ├── __init__.py
│   └── check.py           # API endpoint для проверки ККТ
├── index.html             # Frontend
├── vercel.json            # Конфигурация Vercel
├── .vercelignore          # Файлы, игнорируемые при деплое
├── requirements.txt       # Python зависимости
└── README.md
```

## Особенности Vercel

### Плюсы ✅
- Бесплатный план (100 GB bandwidth/месяц)
- Автоматический HTTPS
- Глобальный CDN
- Автодеплой из GitHub
- Serverless Functions (Python)
- Нет необходимости в сервере

### Ограничения ⚠️
- Максимум 10 секунд на выполнение функции
- Холодный старт (первый запрос может быть медленнее)
- Лимит 12 запросов в секунду (serverless)

## Кастомный домен (опционально)

Хотите использовать свой домен? Легко!

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи (Vercel покажет инструкции)
4. Готово! HTTPS настроится автоматически

## Мониторинг

В Vercel Dashboard вы можете:
- 📊 Смотреть аналитику посещений
- 🐛 Проверять логи ошибок
- ⚡ Отслеживать производительность
- 📈 Смотреть использование ресурсов

## Troubleshooting

### Проблема: API не работает
**Решение**: Проверьте логи в Vercel Dashboard → Functions → Logs

### Проблема: Долгая загрузка
**Решение**: Первый запрос после простоя может быть медленным (холодный старт). Последующие будут быстрее.

### Проблема: Ошибка 500
**Решение**: Проверьте, что все зависимости из `requirements.txt` установлены корректно.

## Поддержка

Если возникли проблемы:
1. Проверьте [документацию Vercel](https://vercel.com/docs)
2. Посмотрите логи в Dashboard
3. Проверьте, что `vercel.json` корректен

---

**Готово!** Ваш сервис проверки ККТ теперь доступен онлайн! 🎉
