# Массовая проверка ККТ в реестре ФНС

Веб-приложение для проверки статуса регистрации контрольно-кассовой техники (ККТ) в реестре ФНС России по заводским номерам.

> **Версия 1.2** - Добавлена поддержка статуса "В реестре утерянных". Приложение теперь различает: ✅ зарегистрированные, ⚠️ в реестре (не зарегистрированные), 🚫 в реестре утерянных, и ❌ не в реестре. Подробности в [CHANGELOG.md](CHANGELOG.md).

## Описание

Официальный сайт ФНС [kkt-online.nalog.ru](https://kkt-online.nalog.ru/#check-kkt) позволяет проверять ККТ только по одному заводскому номеру за раз. Это приложение решает эту проблему, позволяя проверять сразу множество касс одной модели.

## Быстрый старт

### Вариант 1: Локальный запуск

```bash
# Клонируйте или скачайте проект
cd fns_kkt_checker

# Запустите скрипт (он автоматически установит зависимости)
./start.sh

# Откройте в браузере: http://localhost:5001
```

### Вариант 2: Деплой на Vercel (онлайн)

Проект полностью готов для деплоя на Vercel. Смотрите подробные инструкции в [DEPLOY.md](DEPLOY.md).

**Кратко:**
1. Создайте репозиторий на GitHub
2. Push код в GitHub
3. Зайдите на [vercel.com](https://vercel.com) и импортируйте репозиторий
4. Готово! 🎉

## Возможности

- ✅ Массовая проверка заводских номеров ККТ
- ✅ Поддержка моделей: БФР-112ФС, MicroPay-ФАС, СК-1
- ✅ Четыре типа статусов: зарегистрирована, в реестре, утеряна, не в реестре
- ✅ Интеллектуальная интерпретация ответов API ФНС
- ✅ Детальная статистика по каждому типу статуса
- ✅ Удобная таблица с цветными badge для статусов
- ✅ Современный адаптивный интерфейс
- ✅ Задержка между запросами для стабильной работы

## Как использовать

### Установка зависимостей

1. Убедитесь, что у вас установлен Python 3.7 или выше
2. Установите необходимые пакеты:

```bash
pip install -r requirements.txt
```

### Запуск приложения

1. Запустите сервер:

```bash
python server.py
```

Или сделайте файл исполняемым и запустите:

```bash
chmod +x server.py
./server.py
```

2. Откройте браузер и перейдите по адресу: `http://localhost:5001`

> **Примечание:** Если порт 5000 занят (например, AirPlay Receiver на macOS), сервер будет запущен на порту 5001.

### Проверка ККТ

1. Выберите модель кассы из выпадающего списка:
   - БФР-112ФС
   - MicroPay-ФАС
   - СК-1

2. Введите заводские номера в текстовое поле, каждый номер на новой строке:
   ```
   12345678
   87654321
   11111111
   ```

3. Нажмите кнопку **"Проверить кассы"**

4. Дождитесь завершения проверки. Приложение покажет:
   - Общее количество проверенных касс
   - Количество зарегистрированных касс (✅)
   - Количество касс в реестре, но не зарегистрированных (⚠️)
   - Количество касс в реестре утерянных (🚫)
   - Количество касс не в реестре (❌)
   - Детальную таблицу с результатами для каждого номера

## Технические детали

### Архитектура

Приложение состоит из двух частей:
1. **Frontend** (index.html) - веб-интерфейс на HTML/CSS/JavaScript
2. **Backend** (server.py) - прокси-сервер на Python Flask

Backend решает проблему CORS и контролирует частоту запросов к API ФНС.

### API ФНС

Backend обращается к публичному API ФНС:

```
GET https://kkt-online.nalog.ru/lkip.html?query=/kkt/model/check&factory_number={номер}&model_code={код}
```

### API нашего сервера

**POST /api/kkt/check** - проверить одну ККТ
```json
{
  "model_code": "0189",
  "factory_number": "12345678"
}
```

**POST /api/kkt/check-batch** - проверить несколько ККТ
```json
{
  "model_code": "0189",
  "factory_numbers": ["12345678", "87654321"]
}
```

### Коды моделей

- **БФР-112ФС**: `0189`
- **MicroPay-ФАС**: `0124`
- **СК-1**: `0244`

### Формат ответа API

API ФНС возвращает нестандартные комбинации `check_status` и текста. Приложение интеллектуально интерпретирует их.

**Пример 1: ✅ Зарегистрирована в налоговых органах**
```json
{
  "status": 1,
  "check_status": 0,
  "check_result": "Экземпляр с данным заводским номером уже используется. Повторная регистрация не допускается."
}
```

**Пример 2: ⚠️ В реестре, но НЕ зарегистрирована**
```json
{
  "status": 1,
  "check_status": 1,
  "check_result": null
}
```

**Пример 3: 🚫 В реестре утерянных**
```json
{
  "status": 1,
  "check_status": 0,
  "check_result": "Экземпляр с данным заводским номером числится в реестре утерянных. Его использование не допускается."
}
```

**Пример 4: ❌ НЕ в реестре**
```json
{
  "status": 1,
  "check_status": 0,
  "check_result": "Заводской номер ККТ отсутствует в реестре произведенных экземпляров ККТ"
}
```

### Четыре типа статусов

**✅ Зарегистрирована**
- Касса произведена, включена в реестр И успешно зарегистрирована в налоговых органах
- Готова к использованию для фискальных операций

**⚠️ В реестре (не зарегистрирована)**
- Касса произведена и включена в реестр ККТ
- НЕ зарегистрирована в налоговых органах
- Требуется регистрация для начала использования

**🚫 В реестре утерянных**
- Касса числится в реестре утерянных ККТ
- Её использование не допускается
- Обратитесь в налоговую для выяснения обстоятельств

**❌ Не в реестре**
- Касса отсутствует в реестре произведенных экземпляров ККТ
- Возможно, номер введен неправильно или касса ещё не произведена

### Особенности

- Между запросами к API ФНС добавлена задержка 300 мс (контролируется на стороне сервера)
- Backend на Python обходит проблему CORS
- Простая установка и запуск
- Красивый современный интерфейс
- Статистика и детальные результаты проверки

## Структура проекта

```
fns_kkt_checker/
├── index.html          # Frontend (HTML + CSS + JavaScript)
├── server.py           # Backend (Python Flask сервер)
├── requirements.txt    # Python зависимости
├── start.sh            # Скрипт для быстрого запуска
├── README.md           # Документация
└── test_results.png    # Пример результатов проверки
```

## Требования

- Python 3.7 или выше
- pip (менеджер пакетов Python)
- Современный веб-браузер
- Подключение к интернету (для доступа к API ФНС)

## Ограничения

- Проверка осуществляется последовательно для каждого номера
- Время проверки зависит от количества номеров (примерно 0.3 сек на номер + время ответа API)
- При большом количестве номеров (>200) рекомендуется разбить на несколько проверок
- Сервер контролирует частоту запросов к API ФНС для предотвращения блокировки

## Лицензия

MIT

## Автор

Создано для упрощения массовой проверки ККТ в реестре ФНС России.

## Отказ от ответственности

Это приложение использует публичное API ФНС России и предназначено исключительно для проверки статуса регистрации ККТ. Все данные берутся из официальных источников ФНС.
